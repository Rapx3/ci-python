dist: trusty
sudo: required

addons:
  sonarcloud:
    organization: rapx3-github
    token:
      secure: ${SONAR_TOKEN}
jdk:
  - oraclejdk8
script:
  - sonar-scanner
cache:
  directories:
    - '$HOME/.sonar/cache'